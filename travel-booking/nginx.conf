server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /book {
        proxy_pass http://travel-backend:5000/book;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
